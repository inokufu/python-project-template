# Note: This file applies to the Python project template itself.
# Projects generated from this template have their own version of this file.

rules:
  unpinned-uses:
    config:
      policies:
        # A version tag is enough: commit hashes are unmaintainable here
        "*": ref-pin